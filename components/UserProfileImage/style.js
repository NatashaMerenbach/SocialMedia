import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
  userImageContainer: {
    borderWidth: 1,
    borderRadius: 65,
    borderColor: '#F35BAC',
    padding: 3,
  },
  image: {
    width: 65,
    height: 65,
  },
});

export default style;
